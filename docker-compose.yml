services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: papolo-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - papolo_data:/data

volumes:
  papolo_data:
